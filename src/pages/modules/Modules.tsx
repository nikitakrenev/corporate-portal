import React from "react";
import { PageContainer } from "../../widgets/page-container";
import { PageHeader } from "../../components/page-header";

export const Modules = () => {
    return (
        <PageContainer>
            <PageHeader title={"Модули"} buttonType={"add"} />
        </PageContainer>
    );
};
